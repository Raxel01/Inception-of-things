Vagrant commands :
   vagrant box : manipulate and manage boxes
   vagrant destroy [ID]: stop[shutdown] &&cleanup Vms and Resources allocated while starting the vm
   vagrant global-status: state of all active vagrant Environ
   vagrant halt [ID]: shutdow the VM vagrant is running
   vagrant init : create a vagrant environment
   vagrant Login: authenticate to varant cloude
   vagrant package: create a snapshot resulting .box file to share with developers
   vagrant port: list of guet ports mapped to host machine ports
   vagrant reload: running halt and up when you did any changes to the vagrant file
         - it does n't run --provision budefault .
   vagrant resume managed machine that was prev suspended
   vagrant snapshot: manage VM snapshots.
   vagrant ssh: connect to the guest machine by SSH
Deployments Doc kubernetes: 
deployments are managinga set of pods to run app workload
* Deployment ensures that only a certain number of Pods are down
* Deployment also ensures that only a certain number of Pods are
  created above the desired number of Pods

* 


usualy onstateLESS apps!
 * roll out : is the process of replacing the old pods by the new version
one by one so the app staysavailable 
most and required importnatn parts:


* spec.selector :  define how the ReplicaSet find which pods to manage 
specify the labels to match

this labelsto matche are specifyed by spec.template 

spec.template: sub fields:
 - 

 ** get replicatSets:  kubectl get rs
 ** kubectl get pods --show-labels
 ** kubectl rollout status deployment/nginx-deployment
 ** kubectl describe deployments


updating a Deployments:

updating deployment do rollout if and only DEPLYMENTS.SPEC.TEMPLATE IS CHANGED

WRITE A DEPLOYMENT SPEC:
    - apiVErsion
    - kind
    - metadata
    - spec

